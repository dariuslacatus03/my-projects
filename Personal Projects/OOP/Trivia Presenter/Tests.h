#pragma once
#include "Service.h"
#include <assert.h>

class Tests
{
public:
	static void testAdd()
	{
		/*Service service;
		Question q1;
		service.addQuestion(q1);
		assert(service.getAllQuestions().at(9).getScore() == 0);*/
	}
	static void testUpdate()
	{
		/*Service service;
		Question q1;
		service.addQuestion(q1);
		assert(service.getAllQuestions().at(9).getScore() == 0);
		service.updateScore("Lacatus Darius", 5);
		assert(service.getAllParticipants().at(0).getScore() == 5);*/
	}
};

